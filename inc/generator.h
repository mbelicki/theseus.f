#pragma once

#include "types.h"
#include "map.h"

void fill_with_maze( TileType * const map_data
                   , const size_t width
                   , const size_t height
                   );
